# @cupel/cli

Stub. The command line lands milestone by milestone: `cupel inspect` in M3, `cupel audit` in M4, `cupel verify` and the writer in M6. No source exists here yet by design; see `BRIEF.md` section 14 for the build order.

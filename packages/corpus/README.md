# @cupel/corpus

Stub. The corpus manifest, runner, and scoring land in milestone M8. Every corpus image will carry required `license` and `source` fields, enforced in CI. Corpus content is CC BY 4.0 (repo root `LICENSE-CORPUS`); code in this package is Apache-2.0.
